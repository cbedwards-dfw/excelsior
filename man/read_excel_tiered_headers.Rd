% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_excel_tiered_headers.R
\name{read_excel_tiered_headers}
\alias{read_excel_tiered_headers}
\title{Read an Excel file with multi-row headers}
\usage{
read_excel_tiered_headers(
  path,
  sheet = 1,
  header_rows,
  first_data_row = NULL,
  final_data_row = NULL,
  sep = "_",
  clean_names = TRUE
)
}
\arguments{
\item{path}{Character. Path to the \code{.xlsx} file.}

\item{sheet}{Integer or character atomic Sheet index to read. Default \code{1}.}

\item{header_rows}{Integer vector. Row numbers (1-indexed, as they appear
in the spreadsheet) that together form the column headers. Default
\code{c(2, 3, 4)}.}

\item{first_data_row}{Integer or \code{NULL}. Row number of the first data
row. If \code{NULL} (default), set to \code{max(header_rows) + 1}.}

\item{final_data_row}{Integer or \code{NULL}. Row number of the last data
row. If \code{NULL} (default), all rows from \code{first_data_row} to
the end of the sheet are returned.}

\item{sep}{Character. Separator used when joining header parts from
different rows. Default \code{"_"}.}

\item{clean_names}{Logical. Resolves issues in which column names are not unique.  If \code{TRUE} (default), column names are
passed through \code{\link[base:make.names]{base::make.names()}} with \code{unique = TRUE} to ensure
they are valid and non-duplicate R names.}
}
\value{
A data frame with one column per spreadsheet column and one row per
data row. Column names are the combined headers derived from
\code{header_rows}.
}
\description{
Reads an Excel worksheet where column headers are spread across multiple
rows, including merged cells. Header rows are combined into a single header
string per column, with consecutive duplicate values collapsed (e.g. a
superheader "TREATY" spanning columns that are individually labelled
"TROLL", "NET", "SPORT" becomes "TREATY_TROLL", "TREATY_NET",
"TREATY_SPORT").
}
\details{
Merged cells are handled via \code{\link[openxlsx2:wb_to_df]{openxlsx2::read_xlsx()}} with
\code{fill_merged_cells = TRUE}, which propagates the anchor cell's value
across the full span of the merge before any header combination is
attempted.
}
\examples{
\dontrun{
# Basic usage: rows 2-4 are headers, data starts at row 5
df <- read_excel_tiered_headers("data.xlsx")

# Custom header rows and separator
df <- read_excel_tiered_headers(
  path        = "data.xlsx",
  header_rows = c(1, 2),
  sep         = "."
)

# Read only a specific range of data rows
df <- read_excel_tiered_headers(
  path           = "data.xlsx",
  hearder_rows = c(2, 3, 4)
  first_data_row = 7,
  final_data_row = 100
)
}
}
